package objects.centroid;

import objects.Instance;

public interface CentroidComputation {

    /**
     * This method calculate the Centroid
     * @param data
     * @return Instance
     */
    public Instance getCentroid (Instance[] data);

}

